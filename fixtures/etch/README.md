# Etch fixtures

Deterministic manual-pipeline fixtures for the Chisel Etch tab.

These fixtures are dry-run data. Public dummy WIFs are included only to test browser-local signing. Do not fund any fixture address.

The browser loads `etch-fixtures.js`; the JSON files are included for shell tests and external tooling.
